<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zoomelectrico.tesis_ucab">

    <uses-permission
        android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClientActivity" />
        <activity
            android:name=".AdminActivity"
            android:label="@string/title_activity_admin" />
        <activity android:name=".WorkerActivity" />
        <activity android:name=".FailBackActivity" />
        <activity android:name=".EncomiendasAdminActivity" />
        <activity android:name=".AddEncomiendaAdminActivity"/>
    </application>

</manifest>